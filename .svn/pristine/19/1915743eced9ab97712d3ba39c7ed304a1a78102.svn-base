/**
 * Created by Administrator on 2016/1/19.
 */

'use strict'
define([],function(){
    angular.module('addressIndexModule',[])
        .controller('addressIndexCtrl',['$scope',function($scope){
            $("[name=selected]").click(function(){
                /*var check = $("[name=selected]");
                for(var i=0 ; i< check.length ; i++ ){
                    if(check[i].checked){
                      var san =   $(".address-info");
                      for(var j = 0; j<san.length; j++){
                          console.log(san);
                      }
                    }
                }*/
                $(".address-info div").remove();
                $(".address-info").append("<div>1111</div>");
            })
        }])
})
